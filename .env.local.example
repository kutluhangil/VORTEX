# Optional — enables AI preset generation in the gallery.
# Without it, the "Generate with AI" panel is hidden entirely.
ANTHROPIC_API_KEY=

# Public site URL — used for canonical/OG/sitemap. Default: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
